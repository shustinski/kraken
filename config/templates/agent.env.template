# Переменные Kraken Agent. Файл не читается автоматически.
#
# Локальный режим (очередь SQLite, loopback) не требует серверного токена.
# Режим воркера сервера требует оба значения ниже и --plugins-config.

# KRAKEN_AGENT_DATA=%USERPROFILE%\.kraken\agent
# KRAKEN_AGENT_TOKEN=
